<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="gr.gdschua.bloodapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">
        <activity
            android:name="gr.gdschua.bloodapp.Activities.SignupActivity"
            android:theme="@style/Theme.BloodDonationApp"
            android:exported="false"
            android:label="@string/signup_button"/>
        <activity
            android:name="gr.gdschua.bloodapp.Activities.LoginActivity"
            android:theme="@style/Theme.BloodDonationApp"
            android:exported="false"
            android:label="@string/login_button"/>
        <activity
            android:name="gr.gdschua.bloodapp.Activities.LauncherActivity"
            android:theme="@style/Theme.BloodDonationApp.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
        android:fullBackupContent=
    </application>

</manifest>